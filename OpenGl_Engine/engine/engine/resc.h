#include <string>
#include <stdlib.h>
#include <stdio.h>
#include <vector>

typedef struct ProxObj ProxObj;

struct ProxObj {
	
	float proxTrigger; // The maximum distance from the player at which the object is rendered.
};

// A bindary search tree of objects that appear
// based on proximity to the player.
class proxtree {

};

// A collection of 
class rblock {
public:
	-
};

class RescManager {
public:


private:
	
};